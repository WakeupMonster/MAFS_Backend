# Dating App Backend (Node.js + Express)

## Setup

1. Clone the repository
2. Copy `.env.example` → `.env`
3. Run:
   npm install
   npm start

## Scripts
npm run lint  
npm run test  
npm run build  

## Health Check
GET /health → { status: "ok" }